import styled from "styled-components";

const AboutPage = styled.main`
  margin-top: 10rem;
`;

const About = () => {
    return(
        <AboutPage>
        <h1>ABOUT PAGE STILL DEVELOPING </h1>
        </AboutPage>
    )
}

export default About;